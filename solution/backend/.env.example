GROQ_API_KEY=your_groq_api_key_here
GROQ_MODEL=llama-3.3-70b-versatile
EMBEDDING_MODEL=all-MiniLM-L6-v2
CHUNK_WINDOW_SECONDS=45
TOP_K=5
MAX_DISTANCE=1.2

# Optional — only for `python -m app.ingest` when YouTube IP-blocks you (cloud IPs, or a
# home IP rate-limited after many requests). Leave blank to fetch directly. Sign up for a
# residential proxy at https://www.webshare.io and paste the credentials here.
WEBSHARE_PROXY_USERNAME=
WEBSHARE_PROXY_PASSWORD=
